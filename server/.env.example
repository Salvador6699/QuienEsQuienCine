PORT=3001
# Optional: Set your Gemini API key (defaults to heuristic referee if empty, or configure in UI Settings)
GEMINI_API_KEY=
# Optional: Set your TMDB API key (defaults to curated 50+ classic movies if empty, or configure in UI Settings)
TMDB_API_KEY=
# Model name (default: gemini-2.5-flash or gemini-3.8-flash)
GEMINI_MODEL=gemini-2.5-flash
